#include<stdio.h>
#include<cs50.h>

int main(void)
{
    char str[100];

    printf("enter the string");
    scanf("%s",str);

    for(int i=0; str[i] != '\0' ;i++)
    {
     printf("%c\n",str[i]);
    }

}
